services:
    ditherproxy:
        container_name: ditherproxy
        restart: unless-stopped
        ports:
            - 3000:3000
        build:
          context: .
          dockerfile: ./dockerfile
        image: ditherproxy
